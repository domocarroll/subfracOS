#!/bin/bash

sudo cp ~/.local/share/subfrac/default/plymouth/* /usr/share/plymouth/themes/subfrac/
sudo plymouth-set-default-theme subfrac

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
