<svg fill="none" height="285" viewBox="0 0 1215 285" width="1215" xmlns="http://www.w3.org/2000/svg">
  <g fill="#000">

    <!-- === S (x=0..150) === -->
    <!-- Top bar: full width -->
    <rect x="0"   y="0"   width="150" height="45"/>
    <!-- Left upper arm -->
    <rect x="0"   y="45"  width="45"  height="75"/>
    <!-- Middle bar -->
    <rect x="0"   y="120" width="150" height="45"/>
    <!-- Right lower arm -->
    <rect x="105" y="165" width="45"  height="75"/>
    <!-- Bottom bar -->
    <rect x="0"   y="240" width="150" height="45"/>

    <!-- === U (x=180..330) === -->
    <!-- Left vertical -->
    <rect x="180" y="0"   width="45"  height="240"/>
    <!-- Right vertical -->
    <rect x="285" y="0"   width="45"  height="240"/>
    <!-- Bottom bar -->
    <rect x="180" y="240" width="150" height="45"/>

    <!-- === B (x=360..510) === -->
    <!-- Left vertical -->
    <rect x="360" y="0"   width="45"  height="285"/>
    <!-- Top bar -->
    <rect x="405" y="0"   width="60"  height="45"/>
    <!-- Top-right vertical -->
    <rect x="465" y="0"   width="45"  height="120"/>
    <!-- Middle bar -->
    <rect x="405" y="120" width="60"  height="45"/>
    <!-- Bottom-right vertical -->
    <rect x="465" y="165" width="45"  height="75"/>
    <!-- Bottom bar -->
    <rect x="405" y="240" width="60"  height="45"/>
    <!-- Bottom-right corner -->
    <rect x="465" y="240" width="45"  height="45"/>

    <!-- === F (x=540..675) === -->
    <!-- Left vertical -->
    <rect x="540" y="0"   width="45"  height="285"/>
    <!-- Top bar -->
    <rect x="585" y="0"   width="90"  height="45"/>
    <!-- Middle bar -->
    <rect x="585" y="120" width="75"  height="45"/>

    <!-- === R (x=705..855) === -->
    <!-- Left vertical -->
    <rect x="705" y="0"   width="45"  height="285"/>
    <!-- Top bar -->
    <rect x="750" y="0"   width="60"  height="45"/>
    <!-- Top-right vertical -->
    <rect x="810" y="0"   width="45"  height="120"/>
    <!-- Middle bar -->
    <rect x="750" y="120" width="105" height="45"/>
    <!-- Kick leg -->
    <rect x="810" y="165" width="45"  height="120"/>

    <!-- === A (x=885..1035) === -->
    <!-- Top bar (peak) -->
    <rect x="885" y="0"   width="150" height="45"/>
    <!-- Left vertical -->
    <rect x="885" y="45"  width="45"  height="240"/>
    <!-- Right vertical -->
    <rect x="990" y="45"  width="45"  height="240"/>
    <!-- Middle bar -->
    <rect x="930" y="135" width="60"  height="45"/>

    <!-- === C (x=1065..1215) === -->
    <!-- Top bar -->
    <rect x="1065" y="0"   width="150" height="45"/>
    <!-- Left vertical -->
    <rect x="1065" y="45"  width="45"  height="195"/>
    <!-- Bottom bar -->
    <rect x="1065" y="240" width="150" height="45"/>

  </g>
</svg>
