#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
subfrac-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/subfrac/toggles/skip-cursor-theme-changes" Cursor
