#!/bin/bash

if (($# == 0)); then
  echo "Usage: subfrac-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="subfrac-launch-webapp $@"

exec subfrac-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"