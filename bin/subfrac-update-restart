#!/bin/bash

if [ "$(uname -r | sed 's/-arch/\.arch/')" != "$(pacman -Q linux | awk '{print $2}')" ]; then
  gum confirm "Linux kernel has been updated. Reboot?" && subfrac-state clear re*-required && sudo reboot now

elif [ -f "$HOME/.local/state/subfrac/reboot-required" ]; then
  gum confirm "Updates require reboot. Ready?" && subfrac-state clear re*-required && sudo reboot now
fi

for file in "$HOME"/.local/state/subfrac/restart-*-required; do
  if [ -f "$file" ]; then
    filename=$(basename "$file")
    service=$(echo "$filename" | sed 's/restart-\(.*\)-required/\1/')
    echo "Restarting $service"
    subfrac-state clear "$filename"
    subfrac-restart-"$service"
  fi
done
